## This folder contains :
### **controller files**
* which hold the main logic for handling requests.  
* Each controller corresponds to a feature or data type and defines what happens when a route is called with a specific HTTP method `GET`, `POST`, `PUT`, `DELETE`. 
* Routes simply direct requests to the appropriate controller, keeping the code organized and separating logic from routing.