************************************************************
`npm install nodemon --save-dev`

* is the same as

`npm install nodemon -D`

- Both install the package as a development dependency (stored in devDependencies in package.json).
************************************************************

