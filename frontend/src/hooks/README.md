## Folder Purpose: 
### Reusable logic functions (custom hooks).
Handles state management, data fetching, or other behaviors, independent of UI.

## Example:
hooks/
├─ useFetch.js
├─ useForm.js


## Usage:
Extract logic from components for reusability and cleanliness. Example: useFetch(url) can be used in multiple components to fetch data from the backend.