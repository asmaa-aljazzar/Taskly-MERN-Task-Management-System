## Folder Purpose: 
### Define navigation paths and route logic for the application.
This folder maps URLs (paths) to pages or views, controlling what the user sees at each location.

## Example:
routes/
├─ AppRoutes.jsx
├─ PrivateRoute.jsx

## Usage:
Any time your app has multiple pages or screens
When you want centralized routing logic, instead of defining routes in multiple places