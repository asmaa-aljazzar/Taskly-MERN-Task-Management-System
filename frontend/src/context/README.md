## Folder Purpose: 
### Global state management.
Used to share data across many components without passing props manually.

## Example:
context/
├─ AuthContext.jsx
├─ ThemeContext.jsx

## Usage:
Store global state like authentication, theme settings, or user preferences. Example: AuthContext provides user data to any component that needs it.