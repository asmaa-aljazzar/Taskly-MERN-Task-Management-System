{
  "name": "taskly-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --names \"CLIENT,SERVER\" -c \"blue,green\" \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "seed": "npm run seed --prefix backend",
    "seed:reset": "npm run seed:reset --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
